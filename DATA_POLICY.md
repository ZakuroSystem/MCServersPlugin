# MCServers Connector データ送信規約

最終更新: 2026年6月28日

MCServers Connectorは、掲載情報の更新と投票報酬の受け渡しのため、設定されたMCServers.jp APIと定期的に通信します。導入するサーバー管理者は、以下の内容を確認してください。

## プラグインから送信する情報

- サーバーの現在人数と最大人数
- サーバーソフトウェアとバージョン
- プラグインのインスタンスを区別するためのランダムID
- 所有確認に使うトークンと、認証後に発行されるサーバーID
- 送信日時
- 投票報酬の取得件数
- 報酬コマンドを実行できたかどうかと、失敗時の短いエラー内容
- ブラウザ認証を実行した場合の8桁番号、プレイヤー名、UUID

接続元IPアドレスは、通常のWebアクセスと同様にAPIサーバー側で確認できます。

## 投票報酬で扱うプレイヤー情報

投票したプレイヤー名は、MCServers.jpからプラグインへ渡します。プラグインは、`vote-commands` に設定されたコマンドの `%PLAYER%` をその名前に置き換えて実行し、成功または失敗の結果を返します。

`/mcservers auth` を実行した場合に限り、認証するプレイヤーの名前とUUIDをMCServers.jpへ送信します。このプラグインの通常動作では、一般の参加者名、チャット、プレイヤーのIPアドレス、インベントリ、実行したコマンドの全文をMCServers.jpへ送信しません。

## 利用目的

受け取った情報は、次の目的に限って使います。

- 掲載サーバーの所有確認
- オンライン状態と人数の表示
- 同じ認証情報を使った不正な複製の検知
- 投票報酬の受け渡しと処理状況の確認
- 導入済みサーバーからのブラウザ認証
- 障害調査と不正アクセス対策

広告配信のために外部事業者へ渡したり、データを販売したりすることはありません。

## 通信と認証

通信先は、設定ファイルの `base-url` で指定したMCServers.jp APIです。HTTPS接続を必須とし、localhostでの動作確認に限ってHTTPを許可しています。所有確認後のリクエストには、サーバーごとのSecretを使った署名を付けます。

ブラウザ認証では、ブラウザ内でRSA鍵を生成します。公開鍵だけをMCServers.jpへ送り、秘密鍵はブラウザ外へ送信しません。プラグインが取得する認証キーは公開鍵で暗号化されているため、サーバー管理者やプラグインだけでは復号できません。8桁番号は一度限りで、発行したブラウザのセッションに固定されます。

`server-secret` と `ownership-token` は公開せず、他のサーバーと共有しないでください。

## 保存と送信の停止

稼働状況、投票報酬の処理結果、セキュリティ記録は、サービス運営と不正防止に必要な範囲で保存します。

送信を止める場合は、プラグインを無効化または削除してください。登録済みデータの確認や削除相談は、MCServers.jpの問い合わせフォームから受け付けます。

## サーバー管理者の責任

導入前に、ここに記載した送信内容がサーバーのプライバシーポリシーや運用方針に合うことを確認してください。プレイヤーへの案内が必要な場合は、サーバー管理者の責任で行ってください。
